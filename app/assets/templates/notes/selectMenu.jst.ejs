<form class="selectMenu">
	<select class="notebook_choice" name="id" autofocus>
		<option selected></option>
		<% notebooks.each(function(notebook) { %>
			<option value="<%= notebook.id %>">
				<%= notebook.escape("title") %>
			</option>
		<% })%>
	</select>
</form>
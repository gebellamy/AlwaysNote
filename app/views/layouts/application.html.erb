<!DOCTYPE html>
<html>
<head>
  <title>AlwaysNote</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  
  
  <% if current_user %>
  	Current User: <%= current_user.username %>
  	Current user authenticated?: <%= authenticated? %>
  	<%= button_to "Logout", new_session_url, :method => :delete %>
  <% else %>
  	Nobody logged in
  	<%= button_to "Login", new_sesion_url, :method => :get %>
  <% end %>
</head>
<body>

<%= yield %>

</body>
</html>
